pipeline {
    agent any

    stages {
        stage('Build') {
            echo 'Build'
        }

        stage('Test') {
            echo 'Test'
        }

        stage('Integrarion Test') {
            echo 'Test'
        }
    }
}
